// Write a swap_columns() to match swap_rows() from §24.5.3. Obviously,
// to do that you have to read and understand some of the existing Matrix
// library code. Don’t worry too much about efficiency: it is not possible to
// get swap_columns() to run as fast as swap_rows().